using UnityEngine;
using System.Collections;

public class TestPositionManager : MonoBehaviour {

    public GameObject obj;
    public GameObject copyobj;

	// Use this for initialization
	void Start () {
        //mainCamera.aspect = 0.6f;
        //mainCamera.ResetAspect();
        //print(mainCamera.aspect);
        //Screen.SetResolution(480, 800, false);
        //print(mainCamera.pixelHeight);
        //print(mainCamera.pixelWidth);
        
        //print(unitSprite.rect.width);
        //print(unitSprite.rect.height);
        //print(unitSprite.bounds.size.y);

        copyobj = obj;
	}
}
